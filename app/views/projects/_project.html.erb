<div class="card">
  <div class="card-body" id="<%= dom_id project %>">
    <h2 class="card-title"><%= project.title %></h2>
    <h6 class="card-subtitle mb-2 text-muted"><%= project.user.email %></h6>
    <p class="card-text"><%= project.description %></p>

  </div>
</div>

